import React from 'react';
import { Outlet } from 'react-router-dom';
const C: React.FC = () => {
    console.log('jinruc');
    return (
        <>
            cccccc
            <Outlet />
        </>
    );
};

export default C;
